# Data Notes

`advisor_profile.json` contains professor-specific metadata from the source CSV.

`value_add_examples.csv` is a small seed dataset used to prove the code path works. It is **not** a claim about Vivek Srikumar's lab results. Replace it with recent lab papers, public benchmark outputs, or an approved dataset before presenting quantitative claims.
